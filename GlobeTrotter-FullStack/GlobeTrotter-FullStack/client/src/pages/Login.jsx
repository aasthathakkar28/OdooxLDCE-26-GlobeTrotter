import {Link,useNavigate} from "react-router-dom";import {useState} from "react";import AuthLayout from "./AuthLayout";import {useAuth} from "../context/AuthContext";export default function Login(){const nav=useNavigate(),{login}=useAuth();const[email,setEmail]=useState(""),[password,setPassword]=useState(""),[error,setError]=useState(""),[busy,setBusy]=useState(false);const submit=async e=>{e.preventDefault();setBusy(true);setError("");try{await login({email,password},"login");nav("/")}catch(x){setError(x.message)}finally{setBusy(false)}};return <AuthLayout title="Welcome back" subtitle="Log in to keep planning your next adventure." footer={<>New to Ekado? <Link to="/signup">Create an account</Link></>}><form onSubmit={submit} className="form auth-form"><label>Username<input value={email} onChange={e=>setEmail(e.target.value)} type="email" placeholder="Enter your email address" required/></label><label>Password<input value={password} onChange={e=>setPassword(e.target.value)} type="password" placeholder="Enter your password" required/></label><div className="form-row"><span></span><button type="button" className="text-button">Forgot password?</button></div>{error&&<div className="error-text">{error}</div>}<button disabled={busy} className="btn btn-primary full">{busy?"Logging in...":"Log in"}</button><p className="demo-text">Demo: demo@globetrotter.com / demo123</p></form></AuthLayout>}