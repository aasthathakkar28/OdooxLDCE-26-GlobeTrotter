import { Wallet, TrendingUp, AlertTriangle } from "lucide-react"; import PageHeader from "../components/common/PageHeader"; import StatCard from "../components/common/StatCard"; import { budgetData } from "../data/mockData";
export default function Budget(){const total=budgetData.reduce((a,b)=>a+b.value,0);return <section className="page"><PageHeader title="Trip budget" subtitle="Track estimated spending and keep your travel plan under control."/><div className="stats-grid three"><StatCard icon="💰" label="Estimated Total" value={`$${total.toLocaleString()}`} hint="For European Escape"/><StatCard icon="📅" label="Average / Day" value="$227" hint="Across 11 days"/><StatCard icon="⚠️" label="Budget Status" value="On Track" hint="$550 remaining"/></div><div className="content-grid two-one"><div className="section-card"><div className="section-title"><div><h2>Cost breakdown</h2><p>Estimated expenses by category.</p></div></div>{budgetData.map(x=><div className="budget-row" key={x.label}><span>{x.label}</span><div className="bar"><i style={{width:(x.value/total*100)+"%"}}/></div><b>${x.value}</b></div>)}</div><div className="section-card"><h2>Budget alert</h2><div className="budget-alert"><AlertTriangle size={24}/><div><b>Day 3 is above average</b><p>Current estimate is $310.</p></div></div><button className="btn btn-outline full">Adjust budget</button></div></div></section>}