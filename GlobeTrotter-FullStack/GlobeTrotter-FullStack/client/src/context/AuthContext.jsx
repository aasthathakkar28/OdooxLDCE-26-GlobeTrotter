import {createContext,useContext,useEffect,useState} from "react";import {authAPI} from "../services/api";const C=createContext();export function AuthProvider({children}){const[user,setUser]=useState(null);const[loading,setLoading]=useState(true);useEffect(()=>{if(!localStorage.getItem("token"))return setLoading(false);authAPI.me().then(x=>setUser(x.user)).catch(()=>localStorage.clear()).finally(()=>setLoading(false))},[]);const login=async(data,mode)=>{const r=mode==="register"?await authAPI.register(data):await authAPI.login(data);localStorage.setItem("token",r.token);setUser(r.user);return r};const logout=()=>{localStorage.clear();setUser(null)};return <C.Provider value={{user,loading,login,logout}}>{children}</C.Provider>}export const useAuth=()=>useContext(C);